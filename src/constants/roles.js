const ROLES = Object.freeze({ GUEST: 'guest', CONTRIBUTOR: 'contributor', CURATOR: 'curator', ADMIN: 'admin', SUPER_ADMIN: 'super_admin' });
module.exports = { ROLES, ROLE_VALUES: Object.values(ROLES) };
